{
  "node_type": "sql_statement",
  "name": "SQL语句",
  "description": "构建SQL查询语句",
  "category": "数据库",
  "version": "1.0.0",
  "entry_file": "node.py",
  "dependencies": [],
  "config_schema": {
    "sql": {"type": "text", "label": "SQL语句"},
    "output_var": {"type": "string", "label": "输出变量"}
  },
  "examples": [
    {
      "title": "查询全部记录",
      "description": "查询 users 表的所有记录",
      "config": {"sql": "SELECT * FROM users", "output_var": "sql_query"}
    },
    {
      "title": "条件查询",
      "description": "查询年龄大于18的用户",
      "config": {"sql": "SELECT name, age FROM users WHERE age > 18 ORDER BY age", "output_var": "sql_query"}
    },
    {
      "title": "聚合统计",
      "description": "按部门统计人数和平均薪资",
      "config": {"sql": "SELECT dept, COUNT(*) as cnt, AVG(salary) as avg_sal FROM employees GROUP BY dept", "output_var": "sql_query"}
    }
  ]
}
