{
  "node_type": "sqlite_execute",
  "name": "SQLite执行",
  "description": "执行SQL语句",
  "category": "数据库",
  "version": "1.0.0",
  "entry_file": "node.py",
  "dependencies": [],
  "config_schema": {
    "connection_name": {"type": "string", "label": "连接名称"},
    "sql_var": {"type": "string", "label": "SQL变量"},
    "output_var": {"type": "string", "label": "输出变量"}
  },
  "examples": [
    {
      "title": "执行查询语句",
      "description": "在 my_db 连接上执行上游 SQL 语句节点产出的查询",
      "config": {"connection_name": "my_db", "sql_var": "sql_query", "output_var": "query_result"}
    },
    {
      "title": "执行建表语句",
      "description": "在 project_db 连接上执行建表操作",
      "config": {"connection_name": "project_db", "sql_var": "create_table_sql", "output_var": "exec_result"}
    }
  ]
}
